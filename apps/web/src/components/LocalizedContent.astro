---
import { localizeHtml } from "../i18n/translate";
import type { Locale } from "../i18n/config";

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;
const content = await Astro.slots.render("default");
---

<Fragment set:html={localizeHtml(content, locale)} />
