---
import { localeRedirectScript, type PublicPagePath } from "../i18n/config";

interface Props {
  path: PublicPagePath;
}

const { path } = Astro.props;
---

<script is:inline set:html={localeRedirectScript(path)}></script>
