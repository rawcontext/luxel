{
  "name": "@luxel/cli",
  "private": true,
  "scripts": {
    "build": "bash scripts/build-universal.sh",
    "package": "bash scripts/package-release.sh",
    "test": "cargo test --all-targets --all-features --locked",
    "lint": "cargo fmt --check && cargo clippy --all-targets --all-features --locked -- -D warnings"
  }
}
