{
  "name": "luxel",
  "private": true,
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "vercel-build": "bun run --cwd apps/web build && rm -rf .vercel/output && mkdir -p .vercel && cp -R apps/web/.vercel/output .vercel/output",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "app:build": "turbo run app:build",
    "app:build:mas": "turbo run app:build:mas",
    "check:codec-licenses": "turbo run check:codec-licenses",
    "check:distribution": "turbo run check:distribution"
  },
  "devDependencies": {
    "turbo": "2.9.18"
  }
}
