{
  "name": "luxel",
  "private": true,
  "packageManager": "bun@1.4.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "vercel-build": "bun run --cwd apps/web build && rm -rf .vercel/output && mkdir -p .vercel && cp -R apps/web/.vercel/output .vercel/output",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "check:duplication": "jscpd apps/macos/Sources apps/macos/Tests apps/macos/Scripts apps/macos/fastlane apps/web/src apps/web/scripts apps/web/astro.config.mjs --exit-code",
    "app:build": "turbo run app:build",
    "app:build:mas": "turbo run app:build:mas",
    "check:codec-licenses": "turbo run check:codec-licenses",
    "check:distribution": "turbo run check:distribution"
  },
  "devDependencies": {
    "jscpd": "5.0.16",
    "turbo": "2.10.11"
  }
}
