{
  "name": "luxel",
  "private": true,
  "packageManager": "pnpm@12.4.1",
  "scripts": {
    "build": "bazel build //:build",
    "vercel-build": "bash tools/bazel/vercel-build.sh",
    "package": "bazel build --config=release //apps/cli:package",
    "test": "bazel test //:tests",
    "lint": "bazel test //:lint",
    "check:duplication": "bazel test //:duplication",
    "app:build": "apps/macos/Scripts/build-luxel-app.sh",
    "app:build:mas": "apps/macos/Scripts/build-luxel-mas-pkg.sh",
    "check:codec-licenses": "bazel test //apps/macos:codec_compliance_tests",
    "check:distribution": "apps/macos/Scripts/check-app-distribution-build-split.sh"
  },
  "devDependencies": {
    "jscpd": "5.0.16",
    "@bazel/bazelisk": "1.28.1"
  }
}
