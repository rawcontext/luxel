name: App Store Reviews

on:
  workflow_dispatch:
  schedule:
    - cron: "0 8 * * *"
      timezone: America/Chicago

concurrency:
  group: app-store-reviews
  cancel-in-progress: false

permissions:
  contents: read
  issues: write

jobs:
  sync:
    name: Sync reviews to issues
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ github.token }}
      APP_STORE_CONNECT_APP_ID: "6800438206"
      APP_STORE_CONNECT_API_KEY_ID: ${{ secrets.APP_STORE_CONNECT_API_KEY_ID }}
      APP_STORE_CONNECT_API_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_API_ISSUER_ID }}
      APP_STORE_CONNECT_API_KEY_BASE64: ${{ secrets.APP_STORE_CONNECT_API_KEY_BASE64 }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Test review sync
        run: ruby .github/scripts/sync_app_store_reviews_test.rb

      - name: Sync App Store reviews
        run: ruby .github/scripts/sync_app_store_reviews.rb
